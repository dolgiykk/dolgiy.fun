# Used by docker compose -f docker-compose.prod.yml
# Copy to project root as `.env` on the server (or export before compose).

DB_DATABASE=dolfun
DB_USERNAME=dolfun
DB_PASSWORD=change-me

VITE_API_URL=/api
VITE_VK_APP_ID=54726663
